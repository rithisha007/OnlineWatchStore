export interface Order {
  id: number; 
  title: string;
  price: number;
  quantity: number;
  fullPrice: string;
  userEmail: number;
  date: string;
  status: string; 
  itemId: number;// Add the status property
}
